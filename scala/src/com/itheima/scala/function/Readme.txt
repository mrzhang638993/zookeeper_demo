#  高阶函数：高阶函数包括如下的几个部分：
1.作为值的函数
2.匿名函数
3.闭包
4.柯里化等等

val list: List[String] = ints.map("""x""" * _ )
对应的_代表的是书写的是输出部分的内容的。左侧的部分是隐藏的。
println((1 to 10).map(x => "*" * x))
println((1 to 10).map("*" * _))

柯里化可以实现函数的简化操作实现的
隐式转换和隐式参数的使用

隐式参数：方法含有带有implicit标记的参数的，这种情况下，编译器会查找缺省值，提供给该方法。








