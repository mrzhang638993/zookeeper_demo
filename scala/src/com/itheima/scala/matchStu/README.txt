scala中的模式匹配：
1.实现scala中的switch语句。scala中可以使用表达式进行switch实现匹配操作的
2.类型查询
3.使用模式匹配快速查询数据

元祖的模式的复杂匹配逻辑：
1.匹配元祖中其中一个元祖是("zhangsan",1)这样的元祖。
2.更加复杂的match匹配操作需要实现。

option类型可以避免为null对应的类型的判断。option类型可以避免null类型的异常操作的。
scala中偏函数可以简化函数的定义。从而实现代码的更加简单的操作的。函数式编程。

偏函数：是被包括在花括号中内部的没有match的一组case语句对应的是一个偏函数的。没有match的一组case语句
偏函数是PartialFunction[A,B]的一个实例。A代表的是输入参数的类型，B代表的是输出参数的类型。







