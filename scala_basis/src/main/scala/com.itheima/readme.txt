scala 中是没有相关的static的机制的。可以使用单例对象的方式实现
单例对象的操作的，对应的是全局唯一的数据的。
使用object的属性的话，直接使用object对象.属性名称既可以完成相关的代码操作实现的。
使用object对象的话,可以完成相关的静态变量和方法的保存的，可以使用来保存对应的常量信息的
这个是一个很好使用和贯彻的机制以及实现操作的。
单例对象对应的是全局是只有一个对象的。
scala中是没有相关的static的代码和实践机制的。
可以使用object对象创建对象的。可以使用object对象来保存对象实例操作的。




